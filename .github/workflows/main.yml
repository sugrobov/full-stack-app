name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true   # убираем warning Node20

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: ''
          MYSQL_DATABASE: store_db_test
          MYSQL_ALLOW_EMPTY_PASSWORD: yes
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install server dependencies
        run: npm ci
        working-directory: server

      - name: Run server lint
        run: npm run lint
        working-directory: server  

      - name: Run migrations for test DB
        env:
          NODE_ENV: test
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_USER: root
          DB_PASSWORD: ''
          DB_NAME: store_db_test
        run: node migrate.js
        working-directory: server

      - name: Run server integration tests
        env:
          NODE_ENV: test
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_USER: root
          DB_PASSWORD: ''
          DB_NAME: store_db_test
        run: npm test
        working-directory: server

      - name: Install client dependencies
        run: npm ci
        working-directory: client

      - name: Run client lint
        run: npm run lint
        working-directory: client

      - name: Run client unit tests
        run: npm run test
        working-directory: client

      # # --- E2E шаги (временно отключены, будут починены отдельно) ---
      # - name: Install root dependencies (Cypress)
      #   run: npm ci
      #
      # - name: Create client .env for E2E
      #   run: echo "VITE_API_URL=/api" > client/.env
      #
      # - name: Start server in test mode
      #   run: |
      #     nohup env NODE_ENV=test DB_HOST=127.0.0.1 DB_PORT=3306 DB_USER=root DB_PASSWORD='' DB_NAME=store_db_test START_SERVER=true npm run start:test --prefix server > /tmp/server.log 2>&1 &
      #     sleep 2
      #     echo "Server PID: $!"
      #     ps aux | grep -E "node.*server" | grep -v grep || echo "Server process not found"
      #
      # - name: Start client dev server
      #   run: |
      #     nohup npm run dev --prefix client > /tmp/client.log 2>&1 &
      #     sleep 2
      #     echo "Client PID: $!"
      #     ps aux | grep -E "node.*vite" | grep -v grep || echo "Client process not found"
      #
      # - name: Wait for servers to be ready
      #   run: |
      #     echo "=== Server log contents ==="
      #     cat /tmp/server.log 2>/dev/null || echo "No server log yet"
      #     echo ""
      #     echo "=== Client log contents ==="
      #     cat /tmp/client.log 2>/dev/null || echo "No client log yet"
      #     echo ""
      #     echo "Waiting for server (port 5000)..."
      #     for i in $(seq 1 30); do
      #       HTTP_CODE=$(curl -s -o /tmp/server-response.json -w "%{http_code}" http://127.0.0.1:5000/api/products 2>/dev/null || echo "000")
      #       echo "Attempt $i: server returned $HTTP_CODE"
      #       if echo "$HTTP_CODE" | grep -qE '^[23]'; then
      #         echo "Server is ready!"
      #         cat /tmp/server-response.json
      #         break
      #       fi
      #       if [ "$i" -eq 30 ]; then
      #         echo "Server failed to start. Full logs:"
      #         echo "=== Server log ==="
      #         cat /tmp/server.log
      #         echo ""
      #         echo "=== Running processes ==="
      #         ps aux | grep node | grep -v grep
      #         exit 1
      #       fi
      #       sleep 2
      #     done
      #     echo "Waiting for client (port 5173)..."
      #     for i in $(seq 1 30); do
      #       HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5173 2>/dev/null || echo "000")
      #       echo "Attempt $i: client returned $HTTP_CODE"
      #       if echo "$HTTP_CODE" | grep -qE '^[23]'; then
      #         echo "Client is ready!"
      #         break
      #       fi
      #       if [ "$i" -eq 30 ]; then
      #         echo "Client failed to start. Full logs:"
      #         echo "=== Client log ==="
      #         cat /tmp/client.log
      #         echo ""
      #         echo "=== Running processes ==="
      #         ps aux | grep node | grep -v grep
      #         exit 1
      #       fi
      #       sleep 2
      #     done
      #
      # - name: Verify API through Vite proxy
      #   run: |
      #     echo "Testing API through Vite proxy..."
      #     curl -s http://127.0.0.1:5173/api/products | head -c 200
      #     echo ""
      #     echo "Proxy test complete."
      #
      # - name: Run Cypress E2E tests
      #   run: npx cypress run
      #   working-directory: .
      #   env:
      #     CYPRESS_baseUrl: http://127.0.0.1:5173
      #     CYPRESS_defaultCommandTimeout: 10000